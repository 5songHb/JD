name: Sync Fork

on:
  schedule:
    - cron: '*/30 * * * *' # every 30 minutes

jobs:
  sync:

    runs-on: ubuntu-latest

    steps:
      - uses: tgymnich/fork-sync@v1.6.3
        with:
          token: ${{ secrets.JD_SYNC }}
          owner: KingRan
          base: master
          head: master
