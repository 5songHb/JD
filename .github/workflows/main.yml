name: Sync Fork

on:
  schedule:
    - cron: '*/30 * * * *' # every 30 minutes

jobs:
  sync:

    runs-on: ubuntu-latest

    steps:
      - uses: glenq/JD
        with:
          token: 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
          owner: glenq
          base: master
          head: master
